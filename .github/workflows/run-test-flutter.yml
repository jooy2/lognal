name: run-test-flutter

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths:
      - packages/flutter/**
      - .github/workflows/run-test-flutter.yml
  workflow_dispatch:

defaults:
  run:
    working-directory: ./packages/flutter

jobs:
  check:
    name: Analyze, format check and publish check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      # `flutter analyze` walks into `example/`, which resolves against the
      # gallery's own pubspec, so its dependencies have to be fetched too.
      - run: flutter pub get
        working-directory: ./packages/flutter/example
      - run: dart format --output=none --set-exit-if-changed .
      - run: flutter analyze
      # `--dry-run` reports what pub.dev would reject, without publishing anything.
      - run: dart pub publish --dry-run

  test:
    name: Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter test

  example:
    name: Build the gallery
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./packages/flutter/example

    steps:
      - uses: actions/checkout@v5
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter build web --release --no-wasm-dry-run
