import{_ as r,C as i,o as l,c as d,j as t,a as o,E as n,an as s}from"./chunks/framework.9A8ojO3o.js";const y=JSON.parse('{"title":"Introduction","description":"What lognal is, why it draws logs on a canvas, what it can do, and which platforms it supports.","frontmatter":{"order":1,"description":"What lognal is, why it draws logs on a canvas, what it can do, and which platforms it supports.","head":[["link",{"rel":"canonical","href":"https://lognal.cdget.com/introduction"}],["link",{"rel":"alternate","hreflang":"en","href":"https://lognal.cdget.com/introduction"}],["link",{"rel":"alternate","hreflang":"ko","href":"https://lognal.cdget.com/ko/introduction"}],["link",{"rel":"alternate","hreflang":"x-default","href":"https://lognal.cdget.com/introduction"}],["meta",{"property":"og:title","content":"Introduction | lognal"}],["meta",{"property":"og:description","content":"What lognal is, why it draws logs on a canvas, what it can do, and which platforms it supports."}],["meta",{"property":"og:url","content":"https://lognal.cdget.com/introduction"}],["meta",{"property":"og:locale","content":"en_US"}],["meta",{"name":"twitter:title","content":"Introduction | lognal"}],["meta",{"name":"twitter:description","content":"What lognal is, why it draws logs on a canvas, what it can do, and which platforms it supports."}]]},"headers":[],"relativePath":"introduction.md","filePath":"en/introduction.md","lastUpdated":1789616101000}'),h={name:"introduction.md"};function p(c,e,u,g,f,w){const a=i("Fw");return l(),d("div",null,[e[21]||(e[21]=t("h1",{id:"introduction",tabindex:"-1"},[o("Introduction "),t("a",{class:"header-anchor",href:"#introduction","aria-label":'Permalink to "Introduction"'},"​")],-1)),e[22]||(e[22]=t("p",null,"lognal is a log viewer that looks and works like a terminal. It draws log output on a canvas, so it keeps up with a fast stream of messages and a long history without slowing the application down.",-1)),e[23]||(e[23]=t("p",null,[o("It ships as two packages built from the same library: "),t("code",null,"lognal"),o(" on npm for the web, and "),t("code",null,"lognal"),o(" on pub.dev for Flutter. The core is one translation of the other, file for file, so the store, the filters, the wrapping, the value capture and the layout behave the same on both sides. What differs is what only one platform has, and every page says so where it comes up.")],-1)),e[24]||(e[24]=t("h2",{id:"what-it-does",tabindex:"-1"},[o("What it does "),t("a",{class:"header-anchor",href:"#what-it-does","aria-label":'Permalink to "What it does"'},"​")],-1)),t("ul",null,[t("li",null,[e[0]||(e[0]=t("strong",null,"Mirror what the application prints.",-1)),e[1]||(e[1]=o(" Record ",-1)),n(a,{js:"console.log, console.warn, console.table, console.group and the other console methods",flutter:"debugPrint, print and the errors the framework reports"}),e[2]||(e[2]=o(", and show them the way browser developer tools do. The original output keeps working. See ",-1)),e[3]||(e[3]=t("a",{href:"/guide/console"},"Capturing output",-1)),e[4]||(e[4]=o(".",-1))]),t("li",null,[e[5]||(e[5]=t("strong",null,"Show typed values.",-1)),e[6]||(e[6]=o()),n(a,{js:"Objects, arrays, Map, Set, errors and DOM elements",flutter:"Lists, maps, sets, errors and your own classes"}),e[7]||(e[7]=o(" are captured when they are logged and open and close with a tap. See ",-1)),e[8]||(e[8]=t("a",{href:"/guide/values"},"Typed values",-1)),e[9]||(e[9]=o(".",-1))]),e[17]||(e[17]=s('<li><strong>Read text files.</strong> Read a log file in chunks, detect its encoding, and turn ANSI escape codes into colors. lognal can also follow a file that keeps growing. See <a href="/guide/text-files">Text files</a>.</li><li><strong>Work like a log viewer.</strong> A toolbar follows new logs, clears the log, scrolls to the top or bottom, turns wrapping on and off, and filters by text and level. The find shortcut searches the log and highlights every match without hiding anything. A status bar counts entries, each entry can show a timestamp, and text can be selected and copied, or copied a whole entry at a time from the menu of that entry. Addresses in the log are links, opened after a confirmation by default. In entry mode, whole entries are selected the way files are, and copied together. See <a href="/guide/viewer">The viewer</a>.</li><li><strong>Accept commands.</strong> An optional input line passes each command to a function you provide and prints the reply. See <a href="/guide/viewer#input-line">Input line</a>.</li>',3)),t("li",null,[e[10]||(e[10]=t("strong",null,"Follow your theme.",-1)),e[11]||(e[11]=o(" Six palettes are built in, ",-1)),e[12]||(e[12]=t("code",null,"auto",-1)),e[13]||(e[13]=o(" follows the platform, and a theme of your own is ",-1)),n(a,{js:"one block of CSS custom properties",flutter:"one call to buildTheme"}),e[14]||(e[14]=o(". See ",-1)),e[15]||(e[15]=t("a",{href:"/guide/theming"},"Themes and fonts",-1)),e[16]||(e[16]=o(".",-1))]),e[18]||(e[18]=t("li",null,[t("strong",null,"Handle Korean and other CJK text."),o(" Wide characters take two cells, Hangul words stay together when a line wraps, the input line works with an input method, and legacy encodings such as EUC-KR are handled. See "),t("a",{href:"/guide/cjk"},"Korean and CJK text"),o(".")],-1))]),e[25]||(e[25]=t("h2",{id:"why-a-canvas",tabindex:"-1"},[o("Why a canvas "),t("a",{class:"header-anchor",href:"#why-a-canvas","aria-label":'Permalink to "Why a canvas"'},"​")],-1)),t("p",null,[e[19]||(e[19]=o("A log viewer that creates one ",-1)),n(a,{js:"DOM element",flutter:"widget"}),e[20]||(e[20]=o(" per line gets slower as the history grows. Every one of them costs memory, layout and paint work, and a list of a hundred thousand lines is expensive even when only forty are on screen.",-1))]),e[26]||(e[26]=s('<p>lognal keeps entries as plain data and draws only the rows that are visible. The work is split into three parts:</p><ol><li>A <code>LogStore</code> holds the entries in the order they were added, up to a limit of 10,000 by default. The oldest entries are dropped once the limit is reached.</li><li>A <code>LogLayout</code> turns entries into rows for the current width. It caches measured lines, so most frames reuse the work of earlier ones.</li><li>A renderer draws the visible rows on a canvas. Any number of new messages between two frames costs one draw.</li></ol><p>The cost of a frame depends on the number of rows on screen, not on the size of the history.</p><div class="lognal-fw" data-fw="js"><p>The browser still does the jobs it does better than a canvas. The log area is a native scroll container, so the mouse wheel, touch and keyboard scrolling work as usual. The input line is a real <code>&lt;textarea&gt;</code>, so the browser handles the caret, paste and IME composition. The toolbar is made of real buttons, and a visually hidden list mirrors the visible entries for screen readers.</p><p>Text on a canvas is not part of the page text. The viewer provides its own selection, copy and filter instead: drag to select, press Ctrl+C or Cmd+C to copy, and use the filter in the toolbar to search. The text is laid out on a grid of cells, so the log area needs a monospace font.</p></div><div class="lognal-fw" data-fw="flutter"><p>The framework still does the jobs it does better than a canvas. The toolbar, the dialogs and the menus are ordinary widgets, the input line is a real text field, so the platform handles the caret, paste and input method, and a semantics node mirrors the visible entries for a screen reader.</p><p>Text on a canvas is not part of the widget tree. The viewer provides its own selection, copy and filter instead: drag to select, use the platform&#39;s copy shortcut, and use the filter in the toolbar to search. The text is laid out on a grid of cells, so the log needs a monospace font.</p></div><h2 id="package-contents" tabindex="-1">Package contents <a class="header-anchor" href="#package-contents" aria-label="Permalink to &quot;Package contents&quot;">​</a></h2><div class="lognal-fw" data-fw="js"><p>lognal is one npm package with three entry points. The core has no runtime dependencies. React is an optional peer dependency, needed only for <code>lognal/react</code>.</p><table tabindex="0"><thead><tr><th>Entry point</th><th>Contents</th></tr></thead><tbody><tr><td><code>lognal</code></td><td>The viewer, the store and layout, console capture, text sources and the Canvas 2D renderer</td></tr><tr><td><code>lognal/react</code></td><td>The <code>LogViewer</code> React component, for React 18 or later</td></tr><tr><td><code>lognal/style.css</code></td><td>The layout of the viewer and its palettes</td></tr></tbody></table></div><div class="lognal-fw" data-fw="flutter"><p>lognal is one pub.dev package with one import, <code>package:lognal/lognal.dart</code>, holding the viewer widget and its controller, the store and layout, the output hooks, the text sources and the painter.</p><p>It depends on <code>package:flutter</code> and nothing else. It is built on <code>package:flutter/widgets.dart</code> alone, with no Material and no Cupertino, so it sits inside a <code>MaterialApp</code>, a <code>CupertinoApp</code> or a bare <code>WidgetsApp</code> without bringing a second design system along.</p></div><h2 id="platform-support" tabindex="-1">Platform support <a class="header-anchor" href="#platform-support" aria-label="Permalink to &quot;Platform support&quot;">​</a></h2><div class="lognal-fw" data-fw="js"><p>lognal targets current versions of Chrome, Edge, Firefox and Safari. The package is compiled to ES2022. A few features depend on newer browser APIs:</p><ul><li><strong>Grapheme clusters.</strong> lognal splits text into user-perceived characters with <code>Intl.Segmenter</code>, available in Chrome 87, Firefox 125 and Safari 14.1. Where it is missing, a built-in fallback joins combining marks, variation selectors, emoji modifiers, zero width joiner sequences and flag pairs to the character before them. <code>setGraphemeSplitter</code> replaces the splitter with your own.</li><li><strong>Following a file.</strong> <code>followTextFile</code> needs a file handle from the File System Access API (<code>showOpenFilePicker</code>). Only Chromium-based browsers provide it, and only on secure pages served over HTTPS or from <code>localhost</code>. <code>readTextFile</code> works in every browser.</li><li><strong>Copying.</strong> <code>copySelection</code> uses the asynchronous Clipboard API on secure pages and falls back to <code>document.execCommand(&#39;copy&#39;)</code> elsewhere.</li><li><strong>IME input on Safari.</strong> Safari up to version 26 fires <code>compositionend</code> before the <code>keydown</code> of the key that commits a composition. The input line accounts for this, so pressing Enter to confirm a Japanese or Chinese candidate does not submit the command.</li></ul></div><div class="lognal-fw" data-fw="flutter"><p>lognal needs Dart 3.8 and Flutter 3.32, and runs on every target Flutter does. A few things depend on the platform:</p><ul><li><strong>Fonts.</strong> With no <code>family</code> of your own, the viewer picks the platform&#39;s monospace font. Flutter for the web has no system fonts to fall back to, so a web application has to bundle a monospace font and name it. See <a href="/guide/theming#fonts">Fonts</a>.</li><li><strong>Opening a file by path.</strong> <code>localTextFile</code> needs <code>dart:io</code>, so it works everywhere except the web. A file picked on the web is read through <code>CallbackTextFile</code> instead, and reading a stream of bytes works everywhere. See <a href="/guide/text-files">Text files</a>.</li><li><strong>Opening a link.</strong> Handing a URL to the platform needs a plugin, which this package does not take. The viewer draws the link and asks, and <code>onOpenLink</code> is where your application answers. See <a href="/guide/viewer#links">Links</a>.</li><li><strong>Legacy encodings.</strong> UTF-8, UTF-16, Windows-1252 and Latin-1 are decoded here. A legacy CJK encoding such as EUC-KR needs a table Dart does not ship, so <code>registerTextDecoder</code> takes one from a package you already depend on. See <a href="/guide/cjk#file-encodings">File encodings</a>.</li><li><strong>Type names in a release web build.</strong> A class with no <code>toJson()</code> and no <code>toString()</code> of its own is shown by its type name, and <code>dart compile js</code> minifies those. Such a value shows as <code>Object</code> rather than a made-up name. See <a href="/guide/values">Typed values</a>.</li></ul></div>',11))])}const b=r(h,[["render",p]]);export{y as __pageData,b as default};
